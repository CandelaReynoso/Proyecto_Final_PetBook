const { Router } = require("express");

const filterProductsController = require('../controllers/filterProductsController');


const app = Router();

// app.get('/', filterProductsController);

module.exports = app;